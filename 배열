
	// 입력받을 변수 선언
	int ari_score, sam_score, joy_score;
	// 입력받기
	scanf("%d %d %d", &ari_score, &sam_score, &joy_score);
	// 평균 구하기
	float avg = (float)(ari_score + sam_score + joy_score)/3;
	printf("%.2f",avg); // 출력하기 %.2f => 소수점 아래 두번쨰 자리까지 표시, avg는 %에 표시
	return 0; // 끝내기

	// 배열 선언 (공간 할당)
	int arr[6]; // 6개의 정수를 배열에 저장
	// 배열 초기화 (값 넣어주기)
	for (int i = 0; i < 6; i++){
		arr[i] = 10 * (i + 1);
	}

	// 선언과 동기에 값 넣어주기
	int arr2[6] = { 1, 2, 3, 4, 5, 6 };
